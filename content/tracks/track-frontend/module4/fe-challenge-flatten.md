# Deep Flatten

Handling nested arrays is a common task in frontend development, especially when dealing with complex data structures from APIs.

## Your Task
Implement a function that flattens an array of arbitrarily nested arrays into a flat array. You should not use the built-in `Array.prototype.flat()`.

Click the **Start Coding Challenge** button to open the IDE and solve the problem!

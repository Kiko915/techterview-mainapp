rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY: Open access for testing (expires in 30 days)
    match /{allPaths=**} {
      allow read, write: if request.time < timestamp.date(2025, 11, 15);
    }
  }
}
